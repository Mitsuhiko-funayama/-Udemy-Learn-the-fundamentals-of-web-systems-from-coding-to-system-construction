<?php
// 理解度チェック

/* 下記配列は$products以下の様になっている！
|*商品名 => [価格、在庫数];
*/
$products = [
    //キー  と  バリュー と言う！
    'table' => [1000, 2],
    'chair' => [500, 4],
    'bed' => [10000, 1],
    'light' => [5000, 1],
];

/* 問題1:商品一覧を以下の文言の様に出力する！
|
|* 商品一覧
|* 'table'は1000円で2個発注
|* 'chair'は500円で4個発注
|* 'bed'は10000円で1個発注
|* 'light'は5000円で1個発注
*/
// 自分で出力
/*echo '<div>商品一覧</div>';
foreach($products as $val){
    echo $products['table'].'は'.[0].'円で' . [1].'個発注';
    print_r($val);
};

/*
|* 問題1:gemini回答出力
*/
foreach($products as $key => $val){
    // $key は商品名
    // $val[0] は価格
    // $val[1] は個数
    echo $key . 'は' . $val[0] . '円で' . $val[1] . '個発注<br>';
};

/*
|*【問題2：商品購入】
|*
|*$cartの品物を買いたいと想定して、
|*$productsの在庫数が足りている場合、足りていない場合に
|以下のような画面を出力する
|*
|*商品購入
|*'table'を一個ください。
|*はい。ありがとうございます。<-足りている場合
|*
|*'bed'を二個ください。
|*すみません。bedは1個しかありません。<-足りていない場合
*/

// 購入希望　商品数
//$key:商品名
//$va[0]:価格
//$va[1]:個数
$cart = [
	'table' => 1,
	'bed' => 2,
];
echo '<div>商品購入</div>';

/* if文を使って以下の問題が成立するように改造してみる
もし、、
$keyを$val[1]個買ったら、はい。ありがとうございます。
$keyを$val[1]個買ったら、すみません。
bedは$val[1]個しかありません。
*/

/* 自分で出力
if(foreach($$cart as $val[1] > 2)) {
	echo 'はい。ありがとうございます。'
} elseif (foreach($cart as $val[1] > 1)) {
	echo 'すみません。'.$key.'は'.$val[1].'個しかありません。'
};
*/

foreach ($products as $key => $val) {
    $need = 2; // 試しに全部「2個」注文してみるとしたら？

    echo "{$key}を{$need}個ください。<br>";

    if ($val[1] >= $need) {
        echo "はい。ありがとうございます。<br><br>";
    } else {
        echo "すみません。{$key}は{$val[1]}個しかありません。<br><br>";
    }
}